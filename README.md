# zombie-defense
a simple tower defense game with zombies as the antagonists

# setup

start lmstudio and load model
$env:LM_STUDIO_API_BASE="http://localhost:1234/v1"
$env:PATH = "C:\Users\catch\.lmstudio\bin;$env:PATH"
aider --model lm_studio/local-model